<%
  let textedEmail = ''
  const formpressText = 'FORMPRESS Open Source Data Collection\r\n\r\n'
  const helloText = 'FORMPRESS - Reset Password\r\n'
  const textBody = 'You have requested to reset your password. You can create a new password by following the link below.\r\n'
  const verifyLink = `${FRONTEND}/resetpassword/${USERID}/${PASSWORDRESETCODE}`
  const footer = '\r\n\r\n\r\n2022 FORMPRESS'

  textedEmail += formpressText
  textedEmail += helloText
  textedEmail += textBody
  textedEmail += verifyLink
  textedEmail += footer
%>
<%- textedEmail %>
