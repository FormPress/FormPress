<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta
          name="description"
          content="FormPress: Modern, Free, Ultra-Fast Form Builder"
  />
  <%- headerAppend %>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500;1,600;1,700&display=swap">
  <style>
    body {display: none;}
    /* Hide the body until the page is loaded and dependency scripts are up and running. */
  </style>
</head>
<body class="static-markup-body">
  <form
    method="POST"
    action="<%= postTarget %>"
    id="FORMPRESS_FORM_<%= FORMID %>"
    encType="multipart/form-data"
  >
    <%- form %>
    <input type="hidden" name="form_starting_time" id="form_starting_time" value="0" />
  </form>
  <% if(showBranding === true) {%>
    <%- include ("branding.tpl.ejs") %>
  <%} else {null} %>
<script>
    var FORMID = "<%= FORMID %>"
    var BACKEND = "<%= BACKEND %>"
    var USERID = "<%= USERID %>"
  </script>
  <script>
    (function(){
      var el = document.getElementById('form_starting_time');
      el.value = Math.floor(Date.now() / 1000)
    })();
  </script>
  <script src="<%= RUNTIMEJSURL %>"></script>
</body>
</html>
